# Frontend Environment Variables

Copy these into your Coolify or local `.env` settings for the Next.js app:

- `NEXT_PUBLIC_API_BASE_URL` — Base URL for the FastAPI backend (`http://localhost:8080` locally).
- `NEXT_PUBLIC_WS_BASE_URL` — WebSocket/SSE host for streaming responses (`ws://localhost:8080` locally).



